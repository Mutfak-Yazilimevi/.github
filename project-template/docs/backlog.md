# Backlog

> Fikir/fırsat/eksik kaydı. `/onboard` (dev-existing-project-onboarding) buraya **yalnız fikir**
> yazar — değişiklik yapmaz. Maddeler PRD'ye ancak **açık direktifle** bağlanır.
> Değer/Efor: Y(üksek)/O(rta)/D(üşük). Durum: `idea` → `selected` → `in-prd` → `done`.

| # | Başlık | Değer | Efor | Kanıt (dosya:satır) | Durum |
| :-- | :--- | :-: | :-: | :--- | :--- |
| <!-- B-001 --> | <fikir/eksik> | O | O | <path:line> | idea |

## Notlar
- Bu dosya hem yeni (intake sonrası fikirler) hem mevcut (onboarding bulguları) projelerde kullanılır.
- Bir madde işe alınacaksa: önce `selected` işaretle → `pm-create-prd` ile PRD'ye bağla (`in-prd`).
